<Page
    x:Class="BassClefStudio.RssReader.Uwp.NavPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BassClefStudio.RssReader.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:winui="using:Microsoft.UI.Xaml.Controls" 
    xmlns:core="using:BassClefStudio.RssReader.Core"
    mc:Ignorable="d"
    Background="{ThemeResource SystemChromeMediumColor}">
    <winui:NavigationView
        PaneDisplayMode="Top"
        MenuItemsSource="{x:Bind ViewModel.NavItems, Mode=OneWay}"
        SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=OneWay}"
        SelectionChanged="NavigationItemChanged">
        <winui:NavigationView.MenuItemTemplate>
            <DataTemplate x:DataType="core:NavigationItem">
                <winui:NavigationViewItem>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="{x:Bind Icon}"/>
                        <TextBlock Text="{x:Bind Name}"/>
                    </StackPanel>
                </winui:NavigationViewItem>
            </DataTemplate>
        </winui:NavigationView.MenuItemTemplate>
        <Frame x:Name="NavFrame"/>
    </winui:NavigationView>
</Page>